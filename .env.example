# ADEM LLM Voice Assistant - Environment Variables
# Скопируйте этот файл в .env и заполните своими ключами

# =====================================
# SPEECH-TO-TEXT (STT) CONFIGURATION
# =====================================

# OpenAI Whisper API
OPENAI_API_KEY=sk-your-openai-api-key-here

# Google Speech-to-Text
GOOGLE_CREDENTIALS_PATH=/path/to/google-credentials.json

# Azure Speech Services
AZURE_SPEECH_KEY=your-azure-speech-key
AZURE_REGION=your-azure-region

# =====================================
# LARGE LANGUAGE MODEL (LLM) CONFIGURATION  
# =====================================

# OpenAI GPT (GPT-4, GPT-3.5-turbo)
# OPENAI_API_KEY уже указан выше

# Google Gemini API
GOOGLE_API_KEY=your-google-gemini-api-key

# Anthropic Claude API
ANTHROPIC_API_KEY=your-anthropic-api-key

# Ollama (local LLM)
OLLAMA_BASE_URL=http://localhost:11434

# =====================================
# TEXT-TO-SPEECH (TTS) CONFIGURATION
# =====================================

# ElevenLabs API
ELEVENLABS_API_KEY=your-elevenlabs-api-key
ELEVENLABS_VOICE_ID=default

# Google TTS (использует тот же GOOGLE_CREDENTIALS_PATH)
# Azure TTS (использует те же AZURE_SPEECH_KEY и AZURE_REGION)

# =====================================
# APPLICATION SETTINGS
# =====================================

# Режим отладки (true/false)
DEBUG=false

# Уровень логирования (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# =====================================
# INTEGRATIONS
# =====================================

# Grasshopper/Rhino Integration
GRASSHOPPER_ENABLED=false
GRASSHOPPER_PORT=8080
GRASSHOPPER_HOST=localhost

# TouchDesigner Integration
TOUCHDESIGNER_ENABLED=false
TOUCHDESIGNER_PORT=9000
TOUCHDESIGNER_OSC_IP=127.0.0.1

# Manim Integration
MANIM_ENABLED=false
MANIM_QUALITY=high
MANIM_OUTPUT_DIR=animations

# WebSocket для real-time коммуникации
WEBSOCKET_ENABLED=false
WEBSOCKET_PORT=8765
WEBSOCKET_HOST=0.0.0.0

# =====================================
# AUDIO CONFIGURATION
# =====================================

# Устройства ввода/вывода (None = использовать по умолчанию)
# Используйте числовые ID устройств или оставьте пустым
AUDIO_INPUT_DEVICE=
AUDIO_OUTPUT_DEVICE=

# Параметры аудио
AUDIO_SAMPLE_RATE=44100
AUDIO_CHANNELS=2
AUDIO_BUFFER_SIZE=2048

# Обработка аудио
ENABLE_NOISE_REDUCTION=true
ENABLE_ECHO_CANCELLATION=true
ENABLE_AUTO_GAIN=true

# =====================================
# PERFORMANCE
# =====================================

# Максимальное количество одновременных запросов
MAX_CONCURRENT_REQUESTS=5

# Таймаут запросов (секунды)
REQUEST_TIMEOUT=30

# Кэширование
CACHE_ENABLED=true
CACHE_TTL=3600

# =====================================
# DIRECTORIES
# =====================================

# Директории (относительно корня проекта)
RECORDINGS_DIR=recordings
TEMP_DIR=temp
LOGS_DIR=logs
DATA_DIR=data
CONFIG_DIR=config

# =====================================
# ПРИМЕЧАНИЯ
# =====================================
# 
# 1. НИКОГДА не коммитьте файл .env в Git!
#    Файл .env должен быть в .gitignore
#
# 2. Минимальная конфигурация для запуска:
#    - OPENAI_API_KEY (для Whisper STT + GPT LLM)
#    - ELEVENLABS_API_KEY (для TTS)
#
# 3. Альтернативные провайдеры:
#    - Можно использовать Google вместо OpenAI
#    - Можно использовать Azure вместо ElevenLabs
#    - Можно использовать Ollama для локального LLM
#
# 4. Получение API ключей:
#    - OpenAI: https://platform.openai.com/api-keys
#    - Google: https://makersuite.google.com/app/apikey
#    - Anthropic: https://console.anthropic.com/
#    - ElevenLabs: https://elevenlabs.io/
#
# 5. Для production использования:
#    - Храните ключи в безопасном менеджере секретов
#    - Используйте переменные окружения контейнера
#    - Ограничьте права доступа к .env файлу (chmod 600)
